{
  "name": "visual-product-matcher",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "npm -w apps/web run dev",
    "build": "npm -w apps/web run build",
    "start": "npm -w apps/web run start",
    "ingest": "npm -w services/ingest run ingest"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
